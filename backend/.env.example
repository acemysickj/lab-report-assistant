# === API 密钥（请替换为自己的密钥） ===
# 支持 Anyscale / DeepSeek / SiliconFlow 等兼容 API
# sk-ant-... → Anthropic 官方
# sk-... (其他) → DeepSeek 等第三方
ANTHROPIC_API_KEY=sk-your-api-key-here

# === 服务器配置 ===
HOST=127.0.0.1
PORT=8000
CORS_ORIGINS=http://localhost:5173

# === 模型选择（可选） ===
# CLAUDE_MODEL=claude-sonnet-4-6
